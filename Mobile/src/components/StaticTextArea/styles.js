import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    alignContent: 'center',
    padding: 30,
    justifyContent: 'center',
  },
  text: { color: '#fff', textAlign: 'center', fontSize: 25 },
});

export default styles;
